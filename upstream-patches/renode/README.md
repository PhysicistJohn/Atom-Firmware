# Renode upstream fixes moved

The canonical Renode patch series, reproducer, verification matrix, and public
PR status moved to the adjacent `TinySA_Twin` repository:

```text
../TinySA_Twin/upstream-patches/renode/
```

The firmware vendor queue retains the cross-project publication decisions.
Renode implementation and emulator-specific upstream material are now owned by
`TinySA_Twin`.
