{
  "name": "atomos-firmware-contract-checks",
  "version": "0.0.0-private",
  "private": true,
  "description": "Pinned host tooling for AtomOS Firmware contract projections",
  "license": "GPL-3.0-or-later",
  "engines": {
    "node": "22.23.1",
    "npm": "10.9.8"
  },
  "packageManager": "npm@10.9.8",
  "scripts": {
    "check": "tools/check.sh",
    "audit": "npm audit --audit-level=high",
    "test:contract": "node tests/host/test_contract.mjs tests/fixtures",
    "typecheck": "tsc --noEmit --strict --target ES2020 --module ES2020 modern/generated/zs407-contract.ts"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
